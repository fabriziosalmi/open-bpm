/// Minimal CLI to run mixi-core BPM detection on a file.
/// Compile: place this in mixi-core as an example and run:
///   cargo run --release --example bpm_cli -- <file.mp3>
///
/// This file should be copied to: mixi/mixi-core/examples/bpm_cli.rs

fn main() {
    eprintln!("Copy this file to mixi/mixi-core/examples/bpm_cli.rs");
    eprintln!("Then run: cd mixi/mixi-core && cargo run --release --example bpm_cli -- <file>");
}
